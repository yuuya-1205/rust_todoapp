//! バイナリ（実行可能ファイル）の起動地点。
//!
//! モジュールの実体はライブラリクレート（`src/lib.rs`）側に移したため、
//! ここではライブラリが公開する入口を呼び出すだけにします。
//! こうしておくと、`tests/` ディレクトリの結合テストからも同じモジュールを
//! 再利用できます（理由は `src/lib.rs` のコメントを参照）。

fn main() {
    // ライブラリクレート `todoapp` が公開している run 関数を呼び出す。
    // cli モジュールの内部構造を意識せずに済むよう、再エクスポートされた入口を使う。
    todoapp::presentation::run();
}
